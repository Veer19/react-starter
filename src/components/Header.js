const Header = ({ userName, setUsername }) => {
  //   const userName = props.userName;
  return (
    <div style={{ padding: "10px", background: "blue", color: "white" }}>Header - {userName}</div>
  );
};

export default Header;
